改动 1：把 memory_write 加入 coding profile

文件：

src/agents/tool-catalog.ts

找到这段：

{
  id: "memory_search",
  label: "memory_search",
  description: "Semantic search",
  sectionId: "memory",
  profiles: ["coding"],
  includeInOpenClawGroup: true,
},
{
  id: "memory_get",
  label: "memory_get",
  description: "Read memory files",
  sectionId: "memory",
  profiles: ["coding"],
  includeInOpenClawGroup: true,
},

改成：

{
  id: "memory_search",
  label: "memory_search",
  description: "Semantic search",
  sectionId: "memory",
  profiles: ["coding"],
  includeInOpenClawGroup: true,
},
{
  id: "memory_write",
  label: "memory_write",
  description: "Write memory entries to the configured memory backend",
  sectionId: "memory",
  profiles: ["coding"],
  includeInOpenClawGroup: true,
},
{
  id: "memory_get",
  label: "memory_get",
  description: "Read memory files",
  sectionId: "memory",
  profiles: ["coding"],
  includeInOpenClawGroup: true,
},

原因：coding profile 现在只列了 memory_search 和 memory_get，没有 memory_write，所以普通会话会把插件里的 memory_write 过滤掉。

改动 2：给普通 write 加 Milvus memory 文件保护

文件：

src/agents/pi-tools.read.ts

这个文件里有 createHostWorkspaceWriteTool()，它最终调用 createHostWriteOperations() 写文件。

2.1 修改 options 类型

找到：

export function createHostWorkspaceWriteTool(root: string, options?: { workspaceOnly?: boolean }) {

改成：

export function createHostWorkspaceWriteTool(
  root: string,
  options?: {
    workspaceOnly?: boolean;
    blockMemoryMarkdownWrites?: boolean;
  },
) {
2.2 把 options 传给 operations

原来是：

const base = createWriteTool(root, {
  operations: createHostWriteOperations(root, options),
}) as unknown as AnyAgentTool;

保持不变即可，因为 options 类型扩展了。

2.3 修改 createHostWriteOperations 类型

找到：

function createHostWriteOperations(root: string, options?: { workspaceOnly?: boolean }) {

改成：

function createHostWriteOperations(
  root: string,
  options?: {
    workspaceOnly?: boolean;
    blockMemoryMarkdownWrites?: boolean;
  },
) {
2.4 在 createHostWriteOperations 上方加辅助函数

放在 createHostWriteOperations() 前面，也就是 writeHostFile() 后面比较合适：

function isBlockedMemoryMarkdownWrite(root: string, filePath: string): boolean {
  const resolvedRoot = path.resolve(root);
  const resolvedPath = path.resolve(expandTildeToOsHome(filePath));
  const relative = path.relative(resolvedRoot, resolvedPath).replace(/\\/g, "/");

  if (relative.startsWith("../") || path.isAbsolute(relative)) {
    return false;
  }

  return /^memory\/[^/]+\.md$/i.test(relative);
}

function createMemoryMarkdownWriteBlockedError(): Error {
  return new Error(
    "Direct writes to memory/*.md are disabled because the active memory backend is memory-milvus. Use memory_write so the memory is stored in Milvus instead.",
  );
}

这个规则会拦截：

memory/2026-05-18.md
/home/rf/.openclaw/workspace/memory/2026-05-18.md

但不会拦截普通项目文件。

2.5 修改非 workspaceOnly 写入分支

找到：

if (!workspaceOnly) {
  // When workspaceOnly is false, allow writes anywhere on the host
  return {
    mkdir: async (dir: string) => {
      const resolved = path.resolve(expandTildeToOsHome(dir));
      await fs.mkdir(resolved, { recursive: true });
    },
    writeFile: writeHostFile,
  } as const;
}

改成：

if (!workspaceOnly) {
  // When workspaceOnly is false, allow writes anywhere on the host
  return {
    mkdir: async (dir: string) => {
      if (options?.blockMemoryMarkdownWrites && isBlockedMemoryMarkdownWrite(root, dir)) {
        throw createMemoryMarkdownWriteBlockedError();
      }
      const resolved = path.resolve(expandTildeToOsHome(dir));
      await fs.mkdir(resolved, { recursive: true });
    },
    writeFile: async (absolutePath: string, content: string) => {
      if (
        options?.blockMemoryMarkdownWrites &&
        isBlockedMemoryMarkdownWrite(root, absolutePath)
      ) {
        throw createMemoryMarkdownWriteBlockedError();
      }
      await writeHostFile(absolutePath, content);
    },
  } as const;
}
2.6 修改 workspaceOnly 写入分支

找到：

writeFile: async (absolutePath: string, content: string) => {
  const relative = toRelativeWorkspacePath(root, absolutePath);
  await (await rootPromise).write(relative, content, { mkdir: true });
},

改成：

writeFile: async (absolutePath: string, content: string) => {
  const relative = toRelativeWorkspacePath(root, absolutePath);

  if (
    options?.blockMemoryMarkdownWrites &&
    /^memory\/[^/]+\.md$/i.test(relative.replace(/\\/g, "/"))
  ) {
    throw createMemoryMarkdownWriteBlockedError();
  }

  await (await rootPromise).write(relative, content, { mkdir: true });
},
改动 3：在 pi-tools.ts 里启用这个保护

文件：

src/agents/pi-tools.ts

这里已经 import 了 createHostWorkspaceWriteTool。

找到普通 host write 创建处：

const wrapped = createHostWorkspaceWriteTool(workspaceRoot, { workspaceOnly });

改成：

const blockMemoryMarkdownWrites =
  isMilvusBackend && !isMemoryFlushRun;

const wrapped = createHostWorkspaceWriteTool(workspaceRoot, {
  workspaceOnly,
  blockMemoryMarkdownWrites,
});

更推荐把变量放在前面已有变量附近。比如这里附近：

const workspaceOnly = fsPolicy.workspaceOnly;

下面加：

const blockMemoryMarkdownWrites =
  isMilvusBackend && !isMemoryFlushRun;

然后创建 write tool 时传进去。

这样普通会话里，如果模型还试图调用：

write -> memory/2026-05-18.md

会直接失败，提示使用 memory_write。

改动 4：sandbox write 也要拦截吗？

你当前主要是在 host workspace 里测试，先改 host write 就够了。

如果以后 sandbox 模式也会写 memory/*.md，还要改：

createSandboxedWriteTool()
createSandboxWriteOperations()

但当前先不动，避免改动范围扩大。

构建测试

改完后执行：

pkill -f openclaw-gateway || true
pkill -f "openclaw.mjs gateway run" || true

pnpm build
node ./openclaw.mjs config validate
node ./openclaw.mjs gateway run